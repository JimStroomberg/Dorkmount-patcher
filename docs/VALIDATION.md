# Validation

## Current status — 2026-09-12

Dorkmount-patcher has been tested on the maintainer’s Dark Mount keyboard with
CachyOS. Ubuntu package checks run in containers; its desktop and keyboard test
is still pending.

| Check | Current evidence |
|---|---|
| Native CachyOS installation and verification | Confirmed by maintainer |
| Dashboard icon, Waiting…, drawing and navigation | Confirmed by maintainer |
| Other keyboard functions and idle/wake | Confirmed by maintainer |
| Restore stock firmware, then reinstall patch | Confirmed by maintainer |
| Disconnect/reconnect and app restart | Confirmed by maintainer, including random disconnects |
| Ubuntu 26.04 package/dependency/container launch | Automated checks pass |
| Ubuntu desktop session, USB access and real update | **Pending** |
| Recovery from every transfer stage or nonbooting firmware | Not established |

These hardware results cover one keyboard setup. The package checksum, exact OS
build and individual interruption stages were not recorded, so they do not
establish recovery from every failed update. Use [TESTING.md](TESTING.md) to
record those details when testing release packages.

## Firmware and automated evidence

The DMR3 Dock firmware has SHA-256
`8aadce6d0e2f89cbd9d9db08c26dac4d16a4ad55f1a0c1e18f589ee228ad2c1d`.
All component hashes and compatible hardware are in [FIRMWARE.md](FIRMWARE.md).

The Dashboard/system-trust integration at `60d4cc1` passes 112 tests and 35
subtests, lint and release metadata checks. Its GitHub Actions package checks
pass for Ubuntu 26.04 and CachyOS. These establish package installation, declared
dependencies, offscreen/X11/headless-Wayland startup and removal in containers;
they do not replace the pending Ubuntu desktop/USB acceptance test.

Original-instruction checks reproduce the exact firmware and pass 246 graphics
transactions (200 malformed), 12 selector transitions, entry/re-entry and the
original LCD GPIO path. Maximum observed stack use is 248 of 1,024 bytes. The
model substitutes external artwork/palette fixtures and does not execute real
USB, bootloader, interrupts or physical display timing.

Generated build/emulator manifests retain `hardware_verified: false` because
those tools perform no hardware acceptance. That flag describes their output,
not the maintainer's separate physical test report above.

The [Dashboard icon preview](images/dashboard-icon.png) is generated by the
firmware model using project-owned icon artwork. It is not a keyboard photo.
The [updater screenshot](images/updater.png) shows the app in demo mode.

## Earlier records

[Dated offline, pipeline and lab records](history/VALIDATION-2026-09.md) preserve
the earlier results and their limits. Their original “hardware pending”
statements describe the evidence available at that time.
